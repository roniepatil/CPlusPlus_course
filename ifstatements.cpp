#include <iostream>
#include <limits>
#include <cmath>


int main() {
    std::string Name;
    std::cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');
    std::getline(std::cin >> std::ws, Name);
    std::cout << x << '\n';
    return 0;

}